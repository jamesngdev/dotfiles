local options = {
  ensure_installed = { "lua", "http", "json" },
  autotag = {
    enable = true,
  },
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
